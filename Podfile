# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'Cockcrow' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for WakeUpper
  pod 'Alamofire'
  pod 'SwiftDate'

  target 'CockcrowTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CockcrowUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
